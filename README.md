 AVB-VerificationDisabled

?? arm64 Android GKI ? AVB ???????,?? **init ??**? **BL (bootloader) ??**???????

? [yangFenTuoZi/DSU-Permissive](https://github.com/yangFenTuoZi/DSU-Permissive)
? AVB ????,??? DSU ???SELinux permissive ??????,?????????
`boot/init_boot/vendor_boot/dtbo` ???????????????

> **????????** ?????????????????????????????????
> recovery/fastboot ???????????????????

## ??????

### 1. Init ??(??)

ramdisk ?? `avbinit` ??? `/init` ???? `avb_interceptor.ko`,???? PID 1 ?
`/proc/bootconfig` ? vbmeta ????,? `libfs_avb` ??? `VerificationDisabled` ????
system/vendor/odm ???? hashtree ???

### 2. BL ??(??)

`abl_patcher/patch_abl_avb` ??? ABL ELF ???,? bootloader ???? AVB ???
?? `verifiedbootstate=green`,????? boot/init_boot ????? ABL ????????
? [gbl_root_canoe](https://github.com/cvhhji/gbl_root_canoe) ?????,??
**???????? AVB ?????,??????? BL ???**?

## ??

### Init ??

1. ramdisk ?? `avbinit` ??? `/init` ???? `avb_interceptor.ko`?
2. first-stage PID 1 ?? `/proc/bootconfig` ?,????????????????:
   `androidboot.verifiedbootstate = "orange"`?
3. PID 1 ???? `vbmeta[_a|_b]` ?,??????????:?? `AVB0` ?,?
   `AvbVBMetaImageHeader.flags` ????? 123 OR `0x02`?
4. AOSP `libfs_avb` ??? AVB handle ??? `VerificationDisabled`,?????? handle
   ??? system/vendor/odm ??? Hashtree?
5. PID 1 ??? exec `/system/bin/init` ??? Hook;?? 120 ??????

?? vbmeta????????????????????

### BL ??

? ABL ELF ??????:

| ?? | ?? |
|------|------|
| ?? AVB ???? | ???????? `EFI_SUCCESS`,?? boot/init_boot/vendor_boot/dtbo ?? |
| ?? verifiedbootstate=green | ? orange/yellow/red ????????? green,?????? |
| NOP ???? | ????? red/error ???????,???????? |

??:ABL ?? `device_state=locked` + `verifiedbootstate=green`(???????),
????????,????????????

### ??? + Green ??

?? gbl_root_canoe ??????,?? `--green-mode` ?? init_boot:
- ramdisk ??? `/avb_keep_green` ????
- `avbinit` ????????? `avb_keep_green=1`
- ???? orange ??,?? ABL ??? green ??
- vbmeta flags ????(`\|= 0x02`),`libfs_avb` ????
- ???? `locked + green`,???? BL ???

## ????

- ??? bootloader ? boot/init_boot/vendor_boot/dtbo ???????????
  (BL ??????????)?
- ????? vbmeta,?????,??? KeyMint/TEE ?????
- ?? first-stage `fs_mgr` ? `/proc/bootconfig` ?? `verifiedbootstate`,?????????
- ?????? `/dev/block/{by-name,bootdevice/by-name}/vbmeta[_a|_b]`?
- ???????????? `android16-6.12`;?????? GKI KMI target?
- ???????????? KMI/vermagic/????? LKM,??? kprobe ??????

## ??

### Init ???? + loader

???? Android DDK ?? `ddk`?LLVM?Bash:

```bash
tools/build.sh                         # ?? android16-6.12
tools/build.sh --target android16-6.12
```

### BL ?? ABL ????

```bash
make abl_patcher
# ?
cd abl_patcher && make
```

???? gcc/clang,??? Android NDK?

GitHub Actions ? `Build android16-6.12` ???????? Android DDK CLI??????????
KO?loader?Android ??????? magiskboot ?? BL ???????

## ??

### Init ??(????,?????)

```bash
tools/patch-init-boot.sh \
  --input init_boot_ksu_patched.img \
  --output init_boot_avb_disabled.img \
  --loader out/avbinit \
  --module out/avb_interceptor-android16-6.12.ko
```

???????(?? green,??? orange):

```bash
tools/patch-init-boot.sh \
  --input init_boot_ksu_patched.img \
  --output init_boot_avb_green.img \
  --loader out/avbinit \
  --module out/avb_interceptor-android16-6.12.ko \
  --green-mode
```

????? `tools/verify-init-boot.sh`??????? `tools/patch-init-boot-android.sh`;??????
?? `--flash` ???????????

### BL ??(ABL ??)

```bash
# 1. ? abl.img ?? ABL ELF(?? gbl_root_canoe ? extractfv)
extractfv abl.img abl.elf

# 2. ???????(gbl_root_canoe)
patch_abl abl.elf abl_relocked.elf

# 3. ?? BL ?? AVB ??(???)
out/patch_abl_avb abl_relocked.elf abl_final.elf

# 4. ??????? abl ??
```

????? [abl_patcher/README.md](abl_patcher/README.md)?

### ????? + AVB ?????

```
ABL (patched: gbl_root_canoe ??? + patch_abl_avb ????)
  ?
  ?? device_state = locked       (???)
  ?? verifiedbootstate = green   (BL ????)
  ?? AVB ?? = ??             (BL ????)
  ?
  ?? ?? init_boot (patched: avbinit + KO, --green-mode)
       ?
       ?? avbinit ?? /avb_keep_green ? ?? KO (avb_keep_green=1)
            ? KO ?? vbmeta ???? (flags|=0x02),??? orange
            ? userspace libfs_avb ?? system/vendor ??
            ? ???? locked + green,????
```

## ?????

?? bootconfig/vbmeta fops ???????? loader ?? DSU-Permissive `0.6.1` ?????;
????? SPDX `GPL-2.0-only`?BL ?? ABL ?????? gbl_root_canoe ????????
???????? GPL-2.0-only?
