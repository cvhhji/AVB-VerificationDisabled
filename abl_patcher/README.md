 BL-Stage ABL AVB Disabler

? Bootloader (BL) ???? AVB 2.0 ??? ABL ???????,?
[gbl_root_canoe](https://github.com/cvhhji/gbl_root_canoe) ???????,
??**???????? AVB ?????**?

## ??

### ??

- gbl_root_canoe ???? ABL **??** `device_state=locked`,? ABL ????? AVB ???
- init ??? `avb_interceptor.ko` ?? PID 1 ? vbmeta ????(`flags |= 0x02`),
  ? userspace `libfs_avb` ?????
- ??? vbmeta ?? `VERIFICATION_DISABLED (0x02)` ??,?? AVB ????
  `locked + verification_disabled = ???? ? ????`?
- ??????? boot/init_boot ?????? ABL ? hashtree ???

### ????

? BL ??? ABL ??????????? PE/COFF (ARM64 EFI) ??? ELF/flat ??:

| ?? | ?? | ??? |
|------|------|--------|
| ?? AVB ???? | ?? `AVB0`/`vbmeta` ???????????,prologue ??? `MOV X0,XZR; RET` | ??????? |
| ?? verifiedbootstate=green | ?? .data ??? boot state ????(`{name*, value}` 16????),? orange/yellow/red ????????? green ????;fallback ? ADRP+ADD ????? | ?????? green |
| NOP ???? | ????? red/error ??????? | ??????? |

**??**:ABL ?? `locked + green`(???????),????????,
???? boot/init_boot/vendor_boot/dtbo ????????

## ???????

- **???** gbl_root_canoe ?????(GBL ?????unlocked?locked?bootstate ?)?
- ???? gbl_root_canoe ??**??**? ABL ?? AVB ???
- ????? `device_state=locked`,????????? + ?? `green`?
- ????,?????

## ??

```bash
cd abl_patcher
make
```

??:gcc/clang?make??????,??? Android NDK?

## ??

### 1. ?? ABL ELF

?? gbl_root_canoe ? `extractfv` ? `abl.img` ??? ABL ELF:

```bash
extractfv abl.img abl.elf
```

### 2. ???????(gbl_root_canoe)

```bash
patch_abl abl.elf abl_relocked.elf
```

### 3. ?? BL ?? AVB ??(???)

```bash
patch_abl_avb abl_relocked.elf abl_final.elf
```

?????????? ABL ? AVB ??(??????? device_state ?? unlocked):

```bash
patch_abl_avb abl.elf abl_avb_disabled.elf
```

### 4. ???????

? `abl_final.elf` ????? `abl.img` ??,?? `abl` ???
???????? gbl_root_canoe ?????

## ?????

```
ABL (patched: fake-relock + AVB disabled)
  ?
  ?? device_state = locked      (???)
  ?? verifiedbootstate = green  (?????)
  ?? AVB ?? = ??            (?????)
  ?
  ?? ?????? init_boot (? avbinit + avb_interceptor.ko)
       ?
       ?? avbinit ?? KO ? ?? PID 1 vbmeta ???? (flags|=0x02)
            ? userspace libfs_avb ?? system/vendor ??
            ? ????
```

### ?? init ????

BL ??????? ABL ? boot/init_boot/vendor_boot/dtbo ????
system/vendor/odm ??????? first-stage init ? `libfs_avb` ??,
??? init ??? `avb_interceptor.ko` ????

????**? BL ??**??(??? init ????),??????????
vbmeta ??(`flags |= 0x02`),? libfs_avb ???? verification disabled?
?????????,? ABL ?????????(??????????)?

## ??????

```
=== AVB BL-Stage Patcher ===
Buffer size: 1234567 bytes (0x12D687)
Load base: 0x0

AVB0 occurrences: 3
Boot-state strings found: 2

[short_circuit] 'AVB0' at offset 0x1A2B3C
  -> function start at 0x1A0000 (ref at 0x1A2B40)
[short_circuit] patched function at 0x1A0000: A9BF7BFD A9005FF6 -> MOV X0,XZR ; RET

[force_green] 'green' at file offset 0x2C3D4E
[force_green] 'orange' at 0x2C3D58, 1 ADRL reference(s)
  -> repointed ADRL at 0x1A0120 to green
[force_green] 'red' at 0x2C3D68, 2 ADRL reference(s)
  -> repointed ADRL at 0x1A0200 to green

[nop_error] NOP branch at 0x1A01F8 -> 0x1A0200 (red handler at 0x1A0200)

=== Patch Summary ===
  Verification functions short-circuited: 1
  State strings forced to green: 3
  Error branches NOPed: 1
  Total patches: 4
```

## ?????

| ?? | ??? | ABL ?? | ?? |
|------|--------|----------|------|
| ?? 15 Pro | PLK110 | PE/COFF ARM64 EFI | ????????(6??) |

## ????

- **?????**:????? ABL ???????????????? ABL ????????
- **????**:????????? ABL (EDK2) ??????????,?? ABL ??
  ??????????????,??????????/???????
- **????? vbmeta**:?????? ABL ???,?? vbmeta ???
- **? gbl_root_canoe ????**:??????,? AVB ??,????????
  ???? unlocked?locked ??????
